name: Generate comfyui_opt.html

on:
  push:
    branches: [main]
    paths: ['comfyui_opt.md']

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Generate comfyui_opt.html
        run: python scripts/generate_pages.py --opt

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add comfyui_opt.html
          if git diff --cached --quiet; then
            echo "No changes to comfyui_opt.html"
            exit 0
          fi
          git commit -m "chore: regenerate comfyui_opt.html"
          git push
